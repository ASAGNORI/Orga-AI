// Tailwind CSS handles theming through tailwind.config.js
// This file is kept for compatibility with any existing imports

const theme = {
  config: {
    initialColorMode: 'light',
    useSystemColorMode: false,
  },
};

export default theme;